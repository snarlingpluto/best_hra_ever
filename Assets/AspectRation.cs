using UnityEngine;

public class AspectRation : MonoBehaviour
{
/*     public Camera MainCamera;
    void Start()
    {
        MainCamera.AspectRation(16 / 9);
    } */

    // Update is called once per frame
    void Update()
    {
        
    }
}
